<h1><%= "General Forum" %></h1>
<%= link_to "Create A New Post", posts_new_path %>
<hr/>
<% @posts.each do |post|%>
    <p>
     <%= post.title %>
    </p>
    <p>
     <%= post.body %>
    </p>
    <div>
      <%= timestampParser(post.created_at) %>
    </div>
    <p><%= post.university %></p>
    <%= link_to "Details", post %>
    <hr/>
    
 
<% end %> 