<div id="posts_entire" class="row">
<h1><%= current_user.university %></h1>
<%= link_to "Create A New Post", posts_new_path,class:'create_general' %>
<hr/>
<% @posts.each do |post|%>
<div class='general_post'>
    <p>
     <h4 class='general_id'><%= post.title %></h4>
    </p>
    <p>
     <%= post.body %>
    </p>
</div>
    <td> 
    <%= pluralize(post.comments.count, "comment") %> 
    </td>
    <br/>
    <td>
       <%= pluralize(post.get_upvotes.size, "Like") %>
    </td>
    
    <div>
      <%= timestampParser(post.created_at) %>
    </div>
    <p>User's college: <%= User.find(post.user_id).university %></p>
    
    <p>
    <%= link_to "Details", post,class:'details_general' %>
    </p>
    <hr/>
<% end %> 
</div>